<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="controllers/uploads/icons8-video-64.ico" type="image/png">
    <title>Add Course Page</title>
</head>
<body>
    <form action="/addCourse" method="post">    
        <label>Course Name</label>
        <input type="text" name="course_name" placeholder="enter course name ..." required />
        <label>Description</label>
        <input type="text" name="course_des" placeholder="enter course description ..." required />
        <label>Date</label>
        <input type="text" name="course_date" placeholder="mm/dd/yyyy 00:00:00" required />
        <label>URL Video</label>
        <input type="text" name="course_url" placeholder="enter url video ..." required />
        <label>URL Image</label>
        <input type="text" name="course_img" placeholder="enter url image ..." required />
        <label>Course like</label>
        <input type="number" name="course_like" value="0" required />
        <label>Category</label>
        <select name="category">
            <% for(let i=0; i < data.length; i++) { %>      
                <option value="<%= data[i]._id%>"><%= data[i].cate_name%></option>
            <% } %>    
        </select>
        <button type="submit">Add</button>
    </form>
</body>
</html>